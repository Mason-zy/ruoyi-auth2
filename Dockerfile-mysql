FROM mysql:8.0

# 设置时区
ENV TZ=Asia/Shanghai

# 复制初始化SQL脚本到容器中
# MySQL官方镜像会自动执行/docker-entrypoint-initdb.d/目录下的所有.sql文件
COPY init.sql /docker-entrypoint-initdb.d/

# 配置MySQL字符集和校对规则
CMD ["mysqld", "--character-set-server=utf8mb4", "--collation-server=utf8mb4_unicode_ci"] 